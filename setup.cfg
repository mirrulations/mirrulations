# Lint configuration for tools run from the repository root (see Makefile).
# Flake8 and pycodestyle must both be configured — they do not share one file.
[flake8]
max-line-length = 88

[pycodestyle]
max-line-length = 88
