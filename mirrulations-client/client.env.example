# Copy to ../env_files/client.env (created by dev_setup.py).
# Path inside the container must match the compose volume for client_keys.json.
CLIENT_KEYS_PATH=/config/client_keys.json
PYTHONUNBUFFERED=TRUE
AWS_ACCESS_KEY=
AWS_SECRET_ACCESS_KEY=
